<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TaskbarLyriz.App.TaskbarLyricsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="TaskbarLyriz taskbar lyrics">
    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Border
        x:Name="OverlaySurface"
        Padding="10,4"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        BorderThickness="0"
        CornerRadius="10">
        <Grid ColumnSpacing="7">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="2" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <FontIcon
                VerticalAlignment="Center"
                FontSize="12"
                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                Glyph="&#xE8D6;" />

            <TextBlock
                x:Name="CurrentLyricText"
                Grid.Column="1"
                VerticalAlignment="Center"
                AutomationProperties.LiveSetting="Polite"
                AutomationProperties.Name="{Binding TaskbarText}"
                FontSize="15"
                FontWeight="SemiBold"
                IsTextSelectionEnabled="False"
                Text="{Binding TaskbarText}"
                TextTrimming="CharacterEllipsis"
                TextWrapping="NoWrap">
                <TextBlock.RenderTransform>
                    <TranslateTransform x:Name="CurrentLyricTranslate" />
                </TextBlock.RenderTransform>
            </TextBlock>

            <ProgressBar
                Grid.Row="1"
                Grid.ColumnSpan="2"
                Height="2"
                IsHitTestVisible="False"
                Maximum="1"
                Value="{Binding LineProgress}" />
        </Grid>
    </Border>
</Window>
